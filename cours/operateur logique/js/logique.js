// alert ("test");

// l'operateur ET ou AND : &&
if((prenomSaisie === prenom) & (ageSaisie === age )){
    // ...code...
    // je ne rentre ic que si les conditions sont vrai
}
if ((A) && (b)){...}
si A est faux et B est vrai => FAUX
si A est vrai et B est faux => FAUX
si A est faux et B est faux => FAUX
si A est vrai et B est vrai => vrai

// l'opretaeur OU ou OR :|| (alt gr + 6)
    // ...code...
    // je ne rentre ic que si les conditions sont vrai

if ((A) && (b)){...}
si A est faux et B est fauc => faux
si A est faux et B est vrai => vrai
si A est vrai et B est faux => vrai
si A est vrai et B est vrai => vrai

/* L'operateur "!" signifie : le contraire de */
var utilisateursLog = true;
if(!utilisateursLog){
    //si l'utilisateur n'est pas loggé.
}
//ce qui revient a ecrire :
if(utilisateursLog == false){
    // si l'utilisateur n'est pas loggé.
}